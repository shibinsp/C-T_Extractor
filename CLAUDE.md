# Project Rules

## Git Commits

- Commit after every individual file change — do not batch multiple file edits into one commit.
- Never add Claude as a co-author. Do not include `Co-Authored-By: Claude` or any `Co-Authored-By: claude` trailer in commit messages.
- Keep commit messages short and descriptive (e.g. `update README`, `fix converter bug`).
